export interface ThemeModule {
  bgImg?: string; // 背景图
  bgColor?: string; // 背景色（纯色）
  textColor?: string; // 文字主色（抽奖结果页专用）
  color?: string; // 主题主色（按钮、标题等）
  subColor?: string; // 辅助色
  listHeaderImg?: string; // 兑奖点列表头图
  materialImg?: string; // 活动详情大图
}
